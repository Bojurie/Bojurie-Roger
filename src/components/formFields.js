import React, { Component } from 'react';

class FormInput extends Component {
    render(){
        return(
            <div className={`${className} form-fields`}>

            </div>
        )
    }
}

export default FormInput;